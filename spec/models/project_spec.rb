require 'spec_helper'

describe Project do
  it "should only allow unique project names"
  it "should allow project names to be changed"
  it "should not allow project names to be changed to already-existing project names"
end
